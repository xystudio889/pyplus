[library]
firstUsedConfig = "If you set this config equal global, the default namespace for pyplus.config() is global."
showDeprecationWarning = "If you set this config equal false,It will cannot output showDeprecationWarning"
[import]
pyscience = "If you set this config equal false,It will speed up the import,but pyplus.science.pyscience needs to be import independently."
math = "If you set this config equal false,It will speed up the import,but advancedlib.math needs to be import independently."
advancedlib = "If you set this config equal false,It will speed up the import,but advancedlib needs to be import independently.Priority is pyscience > math > advancedlib."
[advancedlib]
showAntigravityWeb = "If you set this config equal true,every time you import this module,antigravity web will be open."
[command]